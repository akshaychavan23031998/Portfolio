# Content editing

Identity and links live in `src/config/site.ts`. Experience, projects, skills, and testimonials live in `src/data`. Keep claims evidence-based, use real local images, provide descriptive alt text, and never add an external URL unless it is verified.
