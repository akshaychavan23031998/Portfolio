# Performance

Server-first rendering, static case studies, local media, responsive image sizing, optimized fonts, no initial social API requests, transform/opacity motion, and reduced-motion paths protect Core Web Vitals. Re-run Lighthouse against a production deployment and tune image encoding based on field data.
