# Deployment

Replace the placeholder domain in `src/config/site.ts`. Run all quality scripts and the production Playwright suite. Import the repository into Vercel, use the Next.js preset, configure server-only contact variables when needed, deploy, then verify all nine routes, image optimization, résumé delivery, metadata, sitemap, robots, and the custom 404.
