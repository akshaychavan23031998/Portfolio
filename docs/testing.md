# Testing

Vitest covers component behavior. Playwright covers desktop and mobile routing, filtering, media, menu keyboard behavior, résumé delivery, 404 behavior, and horizontal overflow. Run tests against the production server for parity with deployment.
